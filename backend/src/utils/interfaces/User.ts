export interface partialUser {
    userId: string|null,
    userUserName: string,
    userEmail: string,
    userHash: string,
    userIsAdmin: boolean
}

export interface User {
    userId: string|null,
    userUserName: string,
    userEmail: string,
    userHash: string,
    userIsAdmin: boolean

}